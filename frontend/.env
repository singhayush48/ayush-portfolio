# Base URL of the backend API. Leave unset in development — Vite proxies
# /api to http://localhost:4000 (see vite.config.ts). Set this to your
# deployed backend URL in production, e.g. https://your-api.onrender.com/api
VITE_API_BASE_URL=http://localhost:4000/api
